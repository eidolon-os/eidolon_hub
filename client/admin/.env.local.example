# Eidolon Admin (Vite) 环境变量
#
# 用法：cp .env.local.example .env.local
# 默认值对应仓库根 .env 的 dev 本机配置（Hub API 8081）。
# 注意：VITE_* 在构建时嵌入 JS，改完需重启 vite dev。

# Hub Admin API 基础路径（client/admin 所有请求会拼接此前缀）
VITE_ADMIN_API_BASE=http://localhost:8081/api/admin
