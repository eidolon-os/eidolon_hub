# LiveKit — 与 compose 中容器编排配套（无宿主机 Nginx 路径、无 TURN TLS 证书）
port: 7880
bind_addresses:
  - 0.0.0.0

keys:
  eidolon: eidolon-livekit-secret-2026

turn:
  enabled: true
  udp_port: 3478

rtc:
  port_range_start: 50000
  port_range_end: 60000
  tcp_port: 7881

logging:
  level: info
  json: false
